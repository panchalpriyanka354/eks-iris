apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  labels:
    app: my-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app-container
        image: 730335461340.dkr.ecr.us-west-2.amazonaws.com/ecr-iris-repo:latest
        ports:
        - containerPort: 80
